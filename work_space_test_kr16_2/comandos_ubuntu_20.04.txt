//brillo

xrandr --output LVDS-1 --brightness 0.7

//siempre que inicies

sudo apt-get update

//
sudo apt-get dist-upgrade
//
sudo apt-get autoremove
sudo apt-get update
sudo apt update
sudo apt upgrade
sudo apt dist-upgrade
apt list --upgradable

//verificar atualizaciones
cat /var/log/apt/history.log

// si hay algun problema ejecutar lo siguiente
source devel/setup.bash

//Para ver que esta publicando 
rostopic list
rostopic echo /nombre_del_tópico

// grabrar pantalla 

//Sin limite de tiempo: 
gsettings set org.gnome.settings-daemon.plugins.media-keys max-screencast-length 0

//60 segundos de duración: 
gsettings set org.gnome.settings-daemon.plugins.media-keys max-screencast-length 60

//comando comenzar y parar
CONTROL SHIFT ALT R